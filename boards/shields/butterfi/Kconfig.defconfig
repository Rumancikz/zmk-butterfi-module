if SHIELD_BUTTERFI_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "butterfi"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_BUTTERFI_LEFT || SHIELD_BUTTERFI_RIGHT

config ZMK_SPLIT
    default y

endif
